name: draftr-devcontainer

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    command: /bin/sh -c "while sleep 1000; do :; done"
    volumes:
      - ..:/workspace:cached
    env_file:
      - .env

  database:
    image: postgres:17
    restart: always
    env_file:
      - .env
